# LF everywhere: the METHODOLOGY.lock hash is computed over file bytes and must be
# identical on Windows working copies and Linux CI checkouts.
* text=auto eol=lf
*.db binary
*.png binary
# Without this, `text=auto` above would rewrite CR/LF bytes inside the compressed
# font tables and every self-hosted face would fail to parse.
*.woff2 binary
